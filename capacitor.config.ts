import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.efemesudiyeli.ionicupload',
  appName: 'ionicrImage',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
